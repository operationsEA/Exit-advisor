export const metadata = {
  title: "Page Not Found | BizForSale",
  robots: {
    index: false,
    follow: false,
  },
};

export default function NotFoundLayout({ children }) {
  return <>{children}</>;
}
