export const metadata = {
  title: "Authenticating | BizForSale",
  robots: {
    index: false,
    follow: false,
  },
};

export default function AuthCallbackLayout({ children }) {
  return <>{children}</>;
}
