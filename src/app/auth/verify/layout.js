export const metadata = {
  title: "Verify Email | BizForSale",
  robots: {
    index: false,
    follow: false,
  },
};

export default function VerifyLayout({ children }) {
  return <>{children}</>;
}
